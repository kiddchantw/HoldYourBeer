# Session: Onboarding Tutorial 新手教學

**Date**: 2025-12-31
**Status**: Planning
**Issue**: N/A
**Contributors**: KiddC, Claude AI
**Branch**: main
**Tags**: #ux, #onboarding, #tutorial, #driver.js

**Categories**: UX, Onboarding, User Experience

---

## Overview

### Goal
為完全新手用戶建立首次登入引導教學，幫助他們快速了解 HoldYourBeer 的核心功能。

### Target Users
- 完全新手（從未使用過此 App）

### Trigger
- 首次登入時自動觸發

### Related Documents
- **Dashboard**: `resources/views/dashboard.blade.php`
- **Beer Creation**: `resources/views/beers/create.blade.php`
- **Charts**: `resources/views/charts/index.blade.php`

---

## Context

### 教學內容範圍

| 步驟 | 頁面 | 教學重點 |
|------|------|----------|
| 1-3 | Dashboard | 啤酒列表、新增按鈕、計數器操作 |
| 4-6 | Charts | 圖表類型、資料說明、時間篩選 |

### 用戶流程

```
首次登入
    ↓
┌─────────────────────────────────────┐
│  歡迎 Modal                         │
│  "歡迎使用 HoldYourBeer！           │
│   要看快速導覽嗎？"                 │
│                                     │
│  [開始導覽]  [稍後再說]             │
└─────────────────────────────────────┘
    ↓ 點「開始導覽」

=== Dashboard 頁面 ===
Step 1: 啤酒收藏列表
        "這是你的啤酒收藏，會顯示你追蹤的所有啤酒"

Step 2: 新增按鈕
        "點這裡可以新增一款啤酒到你的收藏"

Step 3: 計數器
        "用 +/- 按鈕記錄你喝了幾杯"

    ↓ 自動跳轉到 Charts 頁面

=== Charts 頁面 ===
Step 4: 圖表區域
        "這裡可以查看你的飲酒統計"

Step 5: 圖表類型切換
        "切換不同的統計圖表：每日/每週/品牌分布等"

Step 6: 時間篩選器
        "選擇要查看的時間範圍"

    ↓
┌─────────────────────────────────────┐
│  完成 Modal                         │
│  "導覽完成！開始記錄你的啤酒吧 🍺"  │
│                                     │
│  [開始使用]                         │
└─────────────────────────────────────┘
```

### 「重新看教學」與「稍後再說」流程

```
用戶登入
    ↓
檢查 onboarding_completed_at
    ↓
┌─────────────────────────────────────────────┐
│ 情境 1: onboarding_completed_at = null      │
│ （從未完成導覽）                             │
└─────────────────────────────────────────────┘
    ↓
顯示歡迎 Modal
    ↓
┌─────────────────┬─────────────────────────┐
│ 點「開始導覽」  │  點「稍後再說」          │
└─────────────────┴─────────────────────────┘
    ↓                       ↓
啟動 Driver.js          關閉 Modal
    ↓                       ↓
完成導覽                不更新資料庫
    ↓                       ↓
更新 onboarding_        下次登入再問
completed_at = now()


┌─────────────────────────────────────────────┐
│ 情境 2: onboarding_completed_at != null     │
│ （已完成導覽）                               │
└─────────────────────────────────────────────┘
    ↓
檢查 email_verified_at
    ↓
┌─────────────────────────────────────────────┐
│ email_verified_at + 30 天 > now()           │
│ （驗證後 30 天內）                           │
└─────────────────────────────────────────────┘
    ↓
在 Navigation 顯示「重新看教學」按鈕
    ↓
點擊按鈕
    ↓
清除 onboarding_completed_at = null
    ↓
跳轉到 Dashboard
    ↓
自動啟動導覽


┌─────────────────────────────────────────────┐
│ email_verified_at + 30 天 <= now()          │
│ （驗證後超過 30 天）                         │
└─────────────────────────────────────────────┘
    ↓
不顯示「重新看教學」按鈕
```

---

## Planning

### 技術選型

| 項目 | 方案 | 說明 |
|------|------|------|
| **JS 庫** | Driver.js | 輕量 (~5KB)、無依賴、支援 highlight + tooltip |
| **首次判斷** | `users.onboarding_completed_at` | 資料庫欄位記錄完成時間 |
| **跨頁導覽** | localStorage | 記錄目前步驟，換頁後繼續 |
| **語系支援** | Laravel i18n | 支援 en / zh-TW |

### Driver.js 選擇原因

| 比較項目 | Driver.js | Intro.js | Shepherd.js |
|----------|-----------|----------|-------------|
| 大小 | ~5KB | ~20KB | ~30KB |
| 依賴 | 無 | 無 | Popper.js |
| 授權 | MIT | AGPL (商用需付費) | MIT |
| 維護 | 活躍 | 活躍 | 活躍 |

### 資料庫變更

```php
// Migration: add_onboarding_completed_at_to_users_table
Schema::table('users', function (Blueprint $table) {
    $table->timestamp('onboarding_completed_at')->nullable()->after('remember_token');
});
```

### 待確認問題

| 問題 | 決定 |
|------|------|
| 導覽完成後，要不要放「重新看教學」按鈕？ | ✅ **要**，提供重新觀看功能 |
| 按鈕位置（Profile / Navigation / Help 頁面）？ | ✅ **Navigation**（主導覽列） |
| 用戶點「稍後再說」後，下次登入還要再問嗎？ | ✅ **要**，持續提醒直到完成導覽 |

### 新增需求

| 需求 | 說明 |
|------|------|
| **按鈕顯示邏輯** | 信箱驗證後一個月內顯示「重新看教學」按鈕 |
| **計算方式** | `email_verified_at + 30 天 > now()` |
| **隱藏條件** | 超過 30 天後自動隱藏按鈕 |

---

## Implementation Checklist

### Phase 1: 基礎建設
- [ ] 安裝 Driver.js (`npm install driver.js`)
- [ ] 建立 Migration：`onboarding_completed_at` 欄位
- [ ] 執行 Migration

### Phase 2: 歡迎 Modal
- [ ] 建立 `OnboardingModal` Livewire 元件
- [ ] 實作「開始導覽」/「稍後再說」邏輯
- [ ] **實作「稍後再說」持續提醒機制**
  - [ ] 點擊「稍後再說」時不更新 `onboarding_completed_at`
  - [ ] 下次登入時再次顯示歡迎 Modal
- [ ] 整合到 Dashboard 頁面（首次登入時顯示）

### Phase 3: Dashboard 導覽 (Step 1-3)
- [ ] 設定 Driver.js 步驟配置
- [ ] Step 1: Highlight 啤酒列表區域
- [ ] Step 2: Highlight 新增按鈕
- [ ] Step 3: Highlight 計數器 (+/- 按鈕)
- [ ] 導覽結束後跳轉到 Charts

### Phase 4: Charts 導覽 (Step 4-6)
- [ ] 偵測 localStorage 繼續導覽
- [ ] Step 4: Highlight 圖表區域
- [ ] Step 5: Highlight 圖表類型切換
- [ ] Step 6: Highlight 時間篩選器
- [ ] 顯示完成 Modal

### Phase 5: 完成處理
- [ ] 更新 `onboarding_completed_at` 欄位
- [ ] 清除 localStorage 導覽狀態
- [ ] 跳轉回 Dashboard

### Phase 6: 「重新看教學」按鈕
- [ ] **在 Navigation 加入「重新看教學」按鈕**
- [ ] **實作顯示邏輯**：
  - [ ] 檢查 `email_verified_at` 是否存在
  - [ ] 計算是否在驗證後 30 天內：`email_verified_at + 30 天 > now()`
  - [ ] 超過 30 天後自動隱藏按鈕
- [ ] **點擊按鈕時**：
  - [ ] 清除 `onboarding_completed_at`（設為 null）
  - [ ] 重新啟動導覽流程
  - [ ] 跳轉到 Dashboard

### Phase 7: 多語系
- [ ] 新增 en 翻譯
- [ ] 新增 zh-TW 翻譯
- [ ] **新增「重新看教學」按鈕翻譯**

### Phase 8: 測試
- [ ] 新用戶首次登入流程
- [ ] **「稍後再說」流程**
  - [ ] 驗證下次登入時再次顯示 Modal
  - [ ] 驗證 `onboarding_completed_at` 保持為 null
- [ ] 跨頁導覽（Dashboard → Charts）
- [ ] **「重新看教學」按鈕測試**
  - [ ] 驗證在 30 天內顯示
  - [ ] 驗證超過 30 天後隱藏
  - [ ] 驗證點擊後重新啟動導覽
- [ ] 多語系切換
- [ ] 行動裝置 RWD

---

## File Structure (預計)

```
resources/
├── js/
│   └── onboarding.js              # Driver.js 配置
├── views/
│   └── livewire/
│       └── onboarding-modal.blade.php
app/
├── Livewire/
│   └── OnboardingModal.php
database/
└── migrations/
    └── xxxx_add_onboarding_completed_at_to_users_table.php
lang/
├── en.json                        # 新增 onboarding 相關翻譯
└── zh-TW.json
```

---

## 技術實作細節

### 「重新看教學」按鈕邏輯

#### 1. 顯示條件

```php
// 在 Navigation Blade 中
@if(Auth::check() && 
    Auth::user()->email_verified_at && 
    Auth::user()->email_verified_at->addDays(30)->isFuture())
    <a href="{{ route('onboarding.restart') }}" class="...">
        {{ __('重新看教學') }}
    </a>
@endif
```

**邏輯說明**：
- ✅ 用戶已登入
- ✅ `email_verified_at` 不為 null（已驗證信箱）
- ✅ 驗證日期 + 30 天 > 現在時間

#### 2. 重新啟動導覽

```php
// OnboardingController.php
public function restart()
{
    Auth::user()->update([
        'onboarding_completed_at' => null
    ]);
    
    return redirect()->route('dashboard')
        ->with('restart_onboarding', true);
}
```

**流程**：
1. 清除 `onboarding_completed_at` 欄位（設為 null）
2. 跳轉到 Dashboard
3. 前端偵測 `restart_onboarding` session，自動啟動導覽

#### 3. 「稍後再說」持續提醒

```php
// OnboardingModal.php (Livewire)
public function skipOnboarding()
{
    // 不更新 onboarding_completed_at
    // 只關閉 Modal
    $this->dispatch('close-modal');
}

public function startOnboarding()
{
    // 啟動導覽
    $this->dispatch('start-driver-tour');
}
```

**邏輯說明**：
- 點「稍後再說」：不更新資料庫，`onboarding_completed_at` 保持 `null`
- 下次登入時：檢查 `onboarding_completed_at === null`，再次顯示 Modal
- 完成導覽：才更新 `onboarding_completed_at = now()`

---

## UI/UX 設計

### 歡迎 Modal 設計

```
┌────────────────────────────────────────┐
│                  🍺                     │
│                                        │
│      歡迎使用 HoldYourBeer！           │
│                                        │
│   想要快速了解如何使用嗎？              │
│   只需要 1 分鐘！                       │
│                                        │
│  ┌──────────────┐  ┌──────────────┐   │
│  │  開始導覽    │  │  稍後再說    │   │
│  └──────────────┘  └──────────────┘   │
└────────────────────────────────────────┘
```

### Driver.js Tooltip 樣式

- 背景：白色 + 陰影
- 標題：深色粗體
- 內容：灰色說明文字
- 按鈕：Amber 主題色（與網站一致）
- 進度：顯示「1/6」步驟指示

---

## Outcome

(待實作後更新)

---

## Lessons Learned

(待實作後更新)

---

## References

### Driver.js
- 官網：https://driverjs.com/
- GitHub：https://github.com/kamranahmedse/driver.js
- 文件：https://driverjs.com/docs/installation

### 相關 Sessions
- `31-footer-rwd.md` - 同日 Session
- `31-profile-ui-changes.md` - 同日 Session

---

**Session 建立時間**: 2025-12-31
**最後更新**: 2025-12-31
