# =============================================================================
# Google OAuth 2.0 設定範本
# =============================================================================
# 請依照以下步驟填入您的實際資訊：
#
# 1. 前往 Google Cloud Console: https://console.cloud.google.com/
# 2. 選擇您的專案
# 3. 前往 "APIs & Services" > "Credentials"
# 4. 找到您建立的 OAuth 2.0 Client ID
# 5. 複製 Client ID 和 Client Secret
# 6. 將下方的值替換成您的實際資訊
# 7. 複製更新後的設定到 .env 檔案中
# =============================================================================

# -----------------------------------------------------------------------------
# 應用程式基本設定
# -----------------------------------------------------------------------------
# 正式網域（使用 HTTPS）
APP_URL=https://holdyourbeers.com

# 如果是測試環境，可以改為：
# APP_ENV=production
# APP_DEBUG=false

# -----------------------------------------------------------------------------
# Google OAuth 2.0 憑證
# -----------------------------------------------------------------------------
# ⚠️ 請替換以下三個值為您的實際資訊

# Google Client ID (格式範例：123456789-xxxxxxxx.apps.googleusercontent.com)
GOOGLE_CLIENT_ID=your_google_client_id_here

# Google Client Secret (格式範例：GOCSPX-xxxxxxxxxxxxx)
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Redirect URI（必須與 Google Cloud Console 設定一致）
GOOGLE_REDIRECT_URI=https://holdyourbeers.com/auth/google/callback

# -----------------------------------------------------------------------------
# 已在 Google Cloud Console 設定的 Authorized Redirect URIs
# -----------------------------------------------------------------------------
# ✅ https://holdyourbeers.com/auth/google/callback
# ✅ https://holdyourbeers.com/en/auth/google/callback
# ✅ https://holdyourbeers.com/zh-TW/auth/google/callback

# =============================================================================
# 設定完成後的步驟
# =============================================================================
# 1. 將上述設定複製到 .env 檔案中（替換原有的 Google 設定）
# 2. 在伺服器上執行：
#    docker-compose -f ../../laradock/docker-compose.yml exec workspace bash -c "cd /var/www/beer/HoldYourBeer && php artisan config:clear"
#    docker-compose -f ../../laradock/docker-compose.yml exec workspace bash -c "cd /var/www/beer/HoldYourBeer && php artisan cache:clear"
# 3. 訪問 https://holdyourbeers.com/en/login 測試
# =============================================================================

# -----------------------------------------------------------------------------
# Apple Sign In（暫時隱藏，未來需要時可設定）
# -----------------------------------------------------------------------------
APPLE_CLIENT_ID=your_apple_client_id
APPLE_CLIENT_SECRET=your_apple_client_secret
APPLE_REDIRECT_URI=https://holdyourbeers.com/auth/apple/callback
